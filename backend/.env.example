# 音乐 API 中转地址（必填）。把真实地址填进 .env（可放 backend/ 或项目根目录），
# 不要把真实地址提交到 git。
MUSIC_API=https://your-api-host.example.com/api

# 网易云登录态 Cookie 文件路径（可选，默认 backend/netease_cookie.json；
# Docker 部署建议挂载数据卷并指向 /data/netease_cookie.json）
# NETEASE_COOKIE_FILE=/data/netease_cookie.json

# 直连网易云官方接口时附加的 X-Real-IP（可选；海外部署遇 412/地域限制时配置，
# 填一个中国大陆 IP）
# NCM_REAL_IP=112.17.8.18
